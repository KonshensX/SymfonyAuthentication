{% extends 'base.html.twig' %}

{% block body %}
    <h1>Posts list</h1>

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Content</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for post in posts %}
            <tr>
                <td><a href="{{ path('post_show', { 'id': post.id }) }}">{{ post.id }}</a></td>
                <td>{{ post.title }}</td>
                <td>{{ post.content }}</td>
                <td>
                
                <a class="btn btn-success" href="{{ path('post_show', { 'id': post.id }) }}">show</a>
                
                <a class="btn btn-success" href="{{ path('post_edit', { 'id': post.id }) }}">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('post_new') }}" class="btn btn-info">Create a new post</a>
        </li>
    </ul>
{% endblock %}
